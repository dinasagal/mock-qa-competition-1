# Test Plan

## Scope

- Validate todo list retrieval
- Validate todo creation
- Validate todo update
- Validate todo deletion

## Execution

1. Install dependencies from `requirements.txt`
2. Run `pytest`
3. Run `pytest --alluredir=allure-results` when Allure results are needed
